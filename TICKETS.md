# Documentation tickets — Rentify_backend

Generated by github-repo-documenter on 2026-09-06. Tracks what was found, what was written, and
what was verified before commit.

| ID | Ticket | Status | Notes |
|----|--------|--------|-------|
| T1 | README: title, status, overview, tech stack, features | Verified | Stack confirmed against `package.json` dependencies; status "Prototype" — no tests, no CI, no route protection |
| T2 | README: getting started + usage | Verified | Commands based on `package.json` `start`/`test` scripts and `.gitignore`'s `.env` entries (`MONGO_URI`, implied `JWT_SECRET`, `PORT` from `db/conn.js`, `utils/jwtToken.js`, `index.js`) |
| T3 | ARCHITECTURE: system overview + component diagram | Verified | Components match actual files: `index.js`, `Routes/*`, `Controllers/*`, `utils/fileUploads.js`, `utils/jwtToken.js` |
| T4 | ARCHITECTURE: DB schema diagram | Verified | Built directly from `Models/user.js` and `Models/house.js`; confirmed no `owner`/`user` field exists on `houseSchema` |
| T5 | ARCHITECTURE: folder-by-folder breakdown | Verified | Cross-checked against actual repo tree (`Controllers/`, `Models/`, `Routes/`, `db/`, `utils/`, `index.js`) |
| T6 | ARCHITECTURE: request lifecycle (sequence diagram) | Verified | Traced against `Controllers/house.js:addHouse` transaction flow and `utils/fileUploads.js` |
| T7 | ARCHITECTURE: known limitations / security findings | Verified | Each claim grounded in file/line: no `verifyToken` usage found repo-wide (grep confirmed), mass assignment in `Controllers/auth.js:10`, CORS config in `index.js:16`, cookie flags in `Controllers/auth.js:35`, hardcoded URL in `Controllers/house.js:140` |

## Summary

Full pass: rewrote README.md, added ARCHITECTURE.md (component diagram, ER diagram, sequence
diagram, folder tour, known limitations) and this ticket log. Repo is a small (4-commit) Express +
MongoDB backend for house listings with no tests or CI. Verified by re-reading every source file
directly (not relying on the implementer's draft): confirmed `verifyToken` is dead code, confirmed
`signup` passes raw `req.body` into `new User(...)`, confirmed CORS and cookie configuration lines,
confirmed there is no `owner` field anywhere in the House schema, and confirmed no `.env` or
credentials are committed in the working tree or git history. Cross-checked against a local clone
of `Rentify_Frontend`: its `BACKEND_API` constant (`http://localhost:3000/api`) and calls to
`/house/houses` and `/house` match this backend's mounted route prefixes exactly. Pushed to branch
`docs/update-readme-architecture`.
